{
  "name": "dodgers-win-alerts",
  "version": "2.0.0",
  "description": "Push-notifies subscribers whenever the LA Dodgers win a home game, using a Cloudflare Worker, ntfy.sh, and the free MLB Stats API.",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "wrangler": "^4.135.0"
  }
}
